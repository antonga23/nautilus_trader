interface:
  display_name: "Parallel Orchestrator"
  short_description: "Coordinate workers with owned parallel slices"
  default_prompt: >-
    Use $parallel-orchestrator to split this task across workers with explicit
    ownership, runner boundaries, and iterative follow-up.
